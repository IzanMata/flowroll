[pytest]
DJANGO_SETTINGS_MODULE = config.settings
# Tests always run with development settings (DJANGO_ENV defaults to "development")
# Set DJANGO_ENV=production explicitly in CI to test production config checks.
python_files = tests/test_*.py
python_classes = Test*
python_functions = test_*
addopts = -v --tb=short
